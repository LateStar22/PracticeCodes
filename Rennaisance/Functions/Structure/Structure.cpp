#include<iostream>
using namespace std;
struct  point{
    int x, y;
};
int main()
{
    //Use Pen and paper to first solve the problem. Then, write the code.
    struct  point p;
    cin >> p.x >> p.y;
    cout << p.x << p.y;
    return 0;
}